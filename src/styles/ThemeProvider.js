import { createMuiTheme } from '@material-ui/core/styles';

export const outerTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#EE4035',
      contrastText: '#fff',
      dark: '#EBB500',
    },
    secondary: {
      main: '#18171D',
      contrastText: '#fff',
      dark: '#18171D',
    },
  },
});
